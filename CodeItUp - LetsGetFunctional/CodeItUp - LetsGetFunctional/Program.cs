using System;

namespace CodeItUp___LetsGetFunctional
{
  public  class Program
    {
        static void Main(string[] args)
        {
           // var sumFunc = Func((int x, int y) => x + y);
        }
    }

    public record CatRecord(string Name, int Age);

}
